# 📜 Wizstream — Changelog

All notable updates to the **Wizstream** Cloudstream extension (and repo-wide
changes), newest first. Moved out of `README.md` in v42 so the README stays a
short newcomer guide.

| Version | Highlights |
|---------|-----------|
| **v56** (2026-07-25) | **One anime source again: the AniList catalogue leaves the unified Wizstream, and the PURE module takes the canonical name "Wizstream-Anime".** (1) Wizstream is now strictly the main catalogue: TMDB movies/TV/drama/cartoons + the Trending Anime row, with StreamPlay-style AniList enrichment on anime pages (tracking ids, banner, JA cast) — the bundled AniList-catalogue provider is removed, so no more doubled anime sources when both extensions are installed. (2) The standalone module formerly known as "Wizstream-AniList" is renamed to **Wizstream-Anime** (pure AniList metadata, zero TMDB — exactly as shipped in v55). Nice side-effect: old bookmarks/continue-watching entries tagged "Wizstream-Anime" from any earlier era bind to this module again. Nothing else changed; the link engine is shared and untouched. Install/update BOTH extensions together |
| **v55** (2026-07-25) | **NEW module: WizstreamAniList — the PURE AniList catalogue (zero TMDB, companion to the unified Wizstream which stays at v54).** On request: a second extension whose metadata comes from AniList ALONE — literally not a single call to api.themoviedb.org anywhere in it: no episode tables, no backdrop/logo swaps, no title lookups. What feeds the page: AniList's Media payload (title, romaji/English/native variants, poster, banner, description, genres, score, trailer, the 25-character Japanese-VA cast, idMal for MAL tracking), AniList's licensed-streaming feed for per-episode titles/thumbnails (per entry, cours-aligned, with the overlong-list guard), and AniList relations for the stacked CircleFTP-style franchise pages (AoT S3 = 22). Honest consequences of the purity you asked for: (a) episodes with no streaming-feed entry show as bare "Episode N" rows with the show poster — there is simply no AniList-sourced title to print; (b) no per-episode air dates / runtimes / ratings (AniList exposes none); (c) no HD logos (AniList has none); (d) Kitsu/Simkl buttons drop off these pages (external services); MAL+AniList tracking are native. The link engine is untouched and shared — the ani.zip ID-MAP call stays ONLY so Cineby/vid-embeds can key by id (it's an id map, not TMDB metadata). Provider registers as "Wizstream-AniList" so it can never clash with the Wizstream-Anime provider inside unified Wizstream; installing this alongside is pointless-duplicate for most people — disable one in source settings if doubled anime rows annoy you |
| **v54** (2026-07-25) | **Unified again + StreamPlay-style catalogue & metadata.** The v51 experiment ends: back to ONE .cs3 / ONE repo entry hosting everything. (1) **The main catalogue is TMDB** and Japanese-animation entries live right inside it — typed as Anime (incl. the v52 rōmaji-vowel search fix, so "haikyuu" headlines the Haikyu!! series), with a new **Trending Anime (JP Animation)** home row off `/discover/tv`. (2) **Anime pages are enriched from AniList exactly the StreamPlay way** — detected from TMDB (genre 16 + JP origin/language), then: MAL/AniList/Kitsu tracking ids via `api.ani.zip`, AniList banner art as the header, and the Japanese voice-actor cast — while the episode TABLE stays TMDB, because its packed-cours numbering (AoT S3 = 22) is provably the shape the BDIX sites use; identity and blurbs no longer have to be stretched across two databases. (3) **The full AniList catalogue ships inside the same package** (nothing lost): stacked CircleFTP-style franchise pages, root titles + root-bound sync, AniList-banner preference, per-entry streaming-feed episode titles (v53), personal watchlist rows — all intact, so old bookmarks of BOTH catalogues keep working. (4) Scraping engine untouched; one version number again. ⚠️ If you installed the separate "Wizstream Anime" extension during the split era (v51-v53), **uninstall it** — it registers the same provider name as the unified package |
| **v53** (2026-07-25) | **Wizstream-Anime is now AniList-metadata everywhere AniList HAS data — episode rows included.** The page itself (title, poster, plot, genres, score, Japanese-VA cast) always came from AniList; only the episode *rows* used TMDB, because AniList's API famously has no episode list. Correction shipped: AniList does carry per-episode titles + thumbnails via its licensed-stream feed (`streamingEpisodes`, per AniList entry) — v53 reads THIS ENTRY's own list first on every row, correctly aligned across merged cours parts (AoT S3 Part 2's ten rows read Part 2's own list, not Season 3's), and only degrades to TMDB names/stills when an entry genuinely has none (older/unlicensed shows). Air dates / runtimes / ratings stay TMDB — AniList exposes no such fields for episodes at all. Guard: AniList sometimes attaches the whole season's list to a split-cour entry; overlong lists are refused so titles can never shift. The TMDB-side Wizstream plugin is untouched and stays at v52 — TMDB metadata is its entire job |
| **v52** (2026-07-25) | **Fix the "This provider does not exist" error + reinstate the v50 features the v51 binary silently lost.** (1) **Root cause of the error page** (the one showing the doubled `…anilist.co/wiz://anilist/104578` address): after the v51 split the anime catalogue is a SEPARATE extension and needs a one-time install per device. Cards saved before the split (bookmarks, continue-watching) call the provider by NAME ("Wizstream-Anime"); when that provider can't be found the app falls back to matching the card's URL against every extension's main URL — and our custom `wiz://anilist/…` addresses can never prefix-match `https://anilist.co`, so nothing resolves. (The doubled URL on the error screen is just app cosmetics when it can't find a provider.) Real fix, two parts: install the **Wizstream Anime** extension once (see v51 note below) — AND from v52 every card carries its REAL URL (`https://anilist.co/anime/<id>`, `https://www.themoviedb.org/movie|tv/<id>`), so the URL fallback resolves even if a stored name ever mismatches. Old saved links in any of the three forms keep opening. (2) **Honest catch**: a workspace sync glitch quietly reverted the two provider source files before the v51 build went out, so the shipped v51 packages did NOT actually contain v50's fixes (franchise-root page titles, root-bound MAL/AniList sync, AniList-banner preference, the "haikyuu" rōmaji-vowel TMDB search fix and JP-animation typing). v52 re-applies every one of them to both providers — verified inside the compiled packages this time, not assumed. Scraping engine untouched |
| **v51** (2026-07-25) | **Back to roots: the unified plugin splits into two simple single-purpose extensions — nothing about scraping changes.** Wizstream was one .cs3 registering two catalogue providers; it is now TWO repo entries you install independently: **Wizstream** (TMDB — movies, TV series, Asian drama, cartoons; the provider file is byte-identical to v50, including the new rōmaji-vowel search fix that keeps "haikyuu" headlining the TV series instead of a recap movie) and **Wizstream-Anime** (AniList — anime/OVA/movies with the stacked CircleFTP-style season pages, franchise-root titles, root-bound MAL/AniList sync and AniList banner art from v49-v50, also untouched). The link engine (`WizstreamSources`: Cineplex BD · FTPBD · Circle FTP · CTGMovies · FM FTP · Mediaserver + Cineby · Bingr · Moonflix + the Vid[x] embed family, and `WizstreamAnimeSources` for the anime-web resolvers) ships inside BOTH plugins, byte-for-byte the same as v50 — the Wizstream module remains its single source of truth and a build task mirrors the copies into the anime module. House rules for updates after you push: the TMDB plugin keeps updating in place as "Wizstream"; anime now arrives as a NEW extension entry "Wizstream Anime" that must be installed once per device (old unified builds lose their anime rows until it is). Icon: pre-existing `icons/WizstreamAnimeIcon.png` put back to use. CI builds both cs3s and refreshes the feed (`builds/plugins.json`) automatically on push |
| **v50** (2026-07-25) | **First user test of the stacked pages — the identity and sync follow-ups.** (1) **Merged pages wear the franchise ROOT title** — a page assembled from 4 AniList entries was named after whichever entry was opened, so "Attack on Titan Season 3 Part 2" showed a page titled "…Season 3 Part 2" while listing Season 1 (25) / 2 (12) / 3 (22) / 4 (28) ("22 episodes with the wrong title"). Like CircleFTP's mega posts, the page is now titled **"Attack on Titan"** / **"Haikyu!!"** whichever season entry you open; single-entry shows keep their own title. (2) **Identity and sync are AniList, blurbs are TMDB — by design, now also by sync binding.** The page title/poster/summary/cast always came from AniList; TMDB only supplies per-episode names/stills/synopses, because the BDIX sites AND TMDB pack cours into one season (site AoT "Season 3" = 22 eps = TMDB s3) while AniList splits them ("Season 3" 12 + "Season 3 Part 2" 10) — AniList literally has no episode list for the merged shape. v50 also rebinds the MAL/AniList sync ids to the FRANCHISE ROOT entry on merged pages (progress lands on AoT S1's tracker entry instead of whichever part you happened to open), and the header art now prefers the **AniList banner** (TMDB backdrop only when AniList has none). Honest limit: a merged page can carry ONE progress number, so watching inside the merged Season 2-4 rows can't advance the separate AniList season entries — that's the price of the CircleFTP-style merged page. (3) **TMDB search: "haikyuu" no longer headlines a movie.** Verified live: TMDB's own multi-search for "haikyuu" returns ONLY compilation movies and stage plays (the show is filed as "Haikyu!!", single-u), so the top card was a 2014 recap MOVIE. The search now retries once with rōmaji long vowels collapsed ("haikyuu" → "haikyu") and promotes the exact-title hit to the front — Haikyu!! the TV series leads, and Japanese-animation results are tagged Anime/AnimeMovie instead of generic TV/Movie. Ordinary titles are untouched (no doubled vowels → no extra request) |
| **v49** (2026-07-25) | **Hardening the new stacked pages against a 10-franchise live-data sweep** (each case replayed against AniList+TMDB before shipping). (1) **\"… Part 1\" starts, never tails** — Jujutsu Kaisen \"The Culling Game Part 1\" is Season 3, not the end of Season 2; only Part 2+ cours fold into their parent pack. (2) **Phantom seasons hidden** — announced sequels with no episode count yet (Frieren Season 3) no longer render as empty season rows. (3) **Unrelated spin-offs gated out** — AniList sometimes wedges an ONA spin-off into a franchise chain (One Piece's \"prequel\" is the 1-episode MONSTERS ONA) which faked a Season 1 and shifted the whole show; chain candidates must now share the franchise's title stem, arc wordings like \"… Mugen Train Arc\" pass naturally. (4) **Arc re-fold by TMDB arithmetic** — when TMDB packs two arc entries into one season and the counts close EXACTLY, the arcs merge into one stacked bucket (verified no-op on Demon Slayer, whose TMDB packing already mirrors AniList: s2=7, s3=11, s4=11, s5=8 — matching the site buckets one-for-one). (5) **Absolute-packed TMDB confirmed on a second show** — Jujutsu Kaisen (all 59 episodes under TMDB \"season 1\") maps stacked-absolute just like Rent-a-Girlfriend. (6) **OVAs opened directly stay single-entry pages** instead of dragging the whole TV chain in; per-season TMDB metadata now fetches in parallel, cutting stacked-page load time |
| **v48** (2026-07-25) | **Stacked franchise pages — Wizstream-Anime multi-season anime now looks and resolves EXACTLY like CircleFTP's mega posts.** (1) **One page, all seasons** — opening ANY entry of a franchise (\"Attack on Titan Season 3\", \"HAIKYU!! 2nd Season\", …) now assembles the whole AniList prequel+sequel chain into ONE page with site-style season buckets matching the pages the user screenshotted from the site: Attack on Titan renders Season 1 (25) · Season 2 (12) · **Season 3 (22 — both cours merged, Part 2 continuing at ep 13)** · Season 4 (28); HAIKYU!! renders Season 1-4 with Season 4 = TO THE TOP's merged 25. Cours parts, OVA link-nodes (Haikyuu S4 chains through an OVA — traversed as bridges, never listed), and AniList's per-entry splits are all handled; single-entry shows render exactly as before. (2) **Every row carries dual identity** — the STACKED season/episode goes to CircleFTP/BDIX/TMDB/embed lookups (provably correct against the user's saved pages: AoT S03E13 = \"The Town Where Everything Began\"), while the OWNING AniList entry's id + its entry-local episode goes to the anime-web sources (AniZone/Allmanga/AniChi), which mirror AniList's split entries. Episode blurbs come one TMDB season fetch per bucket, and **absolute-packed TMDB shows** (Rent-a-Girlfriend files all 60 episodes under TMDB \"season 1\" — the cause of the bare \"Episode N\" rows) are remapped by absolute index; entries ani.zip doesn't map to a TMDB id get a soft bare-title TMDB search. (3) **CircleFTP anime-category browse rescue** — the saved pages proved the server-side search cannot be steered from outside Bangladesh (Haikyuu/Final/Part-2 kept failing despite every widening), so for series requests the resolver now walks the site's own anime catalogue pages (categoryExact=21, the standalone's \"Anime Series\" row) and runs the same strict→fuzzy→tier-3 gates client-side — exactly what browsing the site by hand does; the listing is session-cached 10 minutes. (4) **Wrong-season trap closed** — a mark-less mega post missing the requested season bucket used to silently serve Season 1's episodes; the single-bucket fallback now only fires on genuinely single-season posts (or explicit \"Season N\" title markers) |
| **v47** (2026-07-25) | **Cours-split anatomy lesson: AoT \"Season 3 Part 2\" now behaves like the second half of Season 3 everywhere (metadata AND links), Haikyuu's CircleFTP search unblocked, and the inflated 25-episode pages fixed.** (1) **Part-2 episode addressing** — AniList splits some seasons into two entries (\"Attack on Titan Season 3\", 12 eps + \"Season 3 Part 2\", 10 eps) while TMDB and the BDIX sites pack those as ONE 22-episode season; Part 2 was being sent to the site's *Season 4* (the Final Season!), which is why CircleFTP failed from part-2 entries while part 1 worked. The franchise walk now records every prequel hop's episode count, so a \"…Part N\" entry collapses onto its parent season and its episodes shift by the previous cours' totals (S3 Part 2 ep 1 → shared Season-3-pack ep 13, verified against TMDB's own numbering). The shift feeds the BDIX and embed lookups; the anime-web resolvers (AniZone/Allmanga/AniChi) intentionally keep raw numbering because those sites mirror AniList's per-entry split. (2) **Episode blurbs follow the same map** — Part 2 rows wear the shared season's correct names/stills/synopses (its ep 1 = TMDB s3 ep 13 \"The Town Where Everything Began\") instead of the season's opening episodes. (3) **\"25 episodes instead of 22\"** — root cause found: AniList's streaming-links field is *whole-temporada* scoped (it reports 25 stream links for BOTH halves of AoT S3), and the episode-count fallback preferred it over AniList's own true total (12 and 10), inflating every split-cour page. The count is now AniList's total whenever it exists — Season 3 shows its real 12 episodes, Part 2 its real 10; 12 + 10 = the 22 that TMDB packs into one season, nothing is lost, each half just lives on its own entry page. (4) **Haikyuu root cause** — the v46 punctuation retry kept AniList's *title-form* season wording, so every variant (\"HAIKYU!! 2nd Season\", \"HAIKYU 2nd Season\") still failed against the site's *file-form* posts (\"Haikyuu Season 2\"): neither string is a substring of the other. CircleFTP's search now also tries the decoration-stripped root of the query itself (\"HAIKYU\"), which the tier-3 rescue matches, and post-side matching learned ordinal wording (\"2nd Season\" = Season 2). **Caveats:** sites that file \"part 2\" as its OWN folder can't be detected from outside Bangladesh — worst case such a post yields nothing rather than the wrong episode; and if CircleFTP still finds no Haikyuu after this build, the show likely isn't in its catalogue — one search inside the standalone CircleFTP extension settles it |
| **v46** (2026-07-25) | **User-reported follow-ups from v45, all three fixed.** (1) **My AniList rows actually appear now** — root cause: the account-repo API used in v45 exists only in Cloudstream ≥ 4.8; on older app builds the class is missing and the safety net silently hid the rows. Login is now detected two ways: the token path (≥4.8, private-list-proof) AND the username path (works on every app generation via a reflection call into the account info that exists in both), with the public-profile list query as fallback — caveat: a *private* AniList list still needs the token path, i.e. app ≥ 4.8. (2) **Episode metadata for sequel seasons was Season 1's** (e.g. AoT S2/S3 Part 1 showing S1's 25 episode titles and stills): the TMDB season fetch was hardcoded to /season/1. It now targets the franchise-mapped season (cours-aware — a "…Part 2" entry maps into the season it splits), and when TMDB has no such season the page simply shows no episode blurbs instead of the wrong ones. (3) **CircleFTP search starved by franchise punctuation** (Haikyuu!! S2/S3 found nothing): the site's substring search chokes on "!!"-style leftovers, so the resolver now retries a zero-hit search once with punctuation stripped ("Haikyuu!!" → "Haikyuu") — matching itself still runs on the untampered title, and the franchise-root passes from v45 keep working on top |
| **v45** (2026-07-25) | **AniList watchlist on the anime homepage + Japanese-only cast + Circle FTP multi-season anime fixed.** (1) **My AniList rows** — while you are logged into AniList inside Cloudstream (Settings → Accounts), the Wizstream-Anime homepage leads with "⏯ Watching — My AniList" and "📋 Plan to Watch — My AniList", pulled from your real account (most recently updated first, cached 5 minutes, exactly the same cards as every other row); logged out, the homepage is byte-identical to before and the feature touches nothing. (2) **Japanese cast only** — anime cast rows no longer fetch or display English voice actors; each character renders exactly one card with its Japanese VA (AniList edge duplicates still merged by character id). (3) **Circle FTP now resolves multi-season anime** — the root cause: AniList lacks stacked series entries, so a sequel season arrives titled "Demon Slayer: Entertainment District Arc" while the site files it as "Demon Slayer", and every match gate (strict *and* fuzzy) rejected it. Two-layer fix: Wizstream-Anime now walks the AniList prequel chain (it already did for season numbers) and forwards every ancestor franchise title as extra BDIX search keys, each extra pass running only while nothing has matched yet; and CircleFTP gained a season-aware rescue tier (the "different scraping logic"): decoration-stripped bare-title containment matching that only fires for series requests, with wrong-season protection still enforced downstream by the episode addressing guards. Mega posts and per-season posts both resolve; movies and regular shows are untouched |
| **v44** (2026-07-25) | **TMDB enrichment for the two new standalones (FM FTP v2 + Mediaserver v2), bare display titles, and an anime-cast fix** (consolidated pre-push build). (1) **Enrichment** — FM FTP enriches by exact TMDB/IMDb ids straight from the site's own detail JSON (zero mismatch risk); Mediaserver by cleaned-title search with a ±1-year pick and a Japanese-preference bias for yearless TV lookups (TMDB otherwise ranks live-action remakes above same-named anime, and BDIX episode posts are overwhelmingly anime). Everything — title logos, cast with photos, trailers, runtimes, full genres, IMDb tracking, per-episode synopses and stills — is *fallback-soft*: a TMDB outage silently keeps the site's own title/poster/plot and never touches which links play; results are session-cached, and Mediaserver even caches "this title isn't on TMDB" so a dead lookup costs one request total. (2) **Where the metadata shows up** — homepage and search rows make ZERO TMDB calls and display the site's own posters (full metadata loads only inside an item, logo-to-cast), and every displayed title is now the bare media title: year, quality/rip/dub tails stripped ("Avatar 2009 1080p Hindi Dubbed" → "Avatar"), with protection for titles that legitimately start with a language word ("Hindi Medium" stays intact). (3) **Wizstream-Anime cast deduplicated** — repeated AniList character edges are merged by character id, and the JA/EN voice-actor split no longer renders twin cards: it is now ONE card per character with the Japanese VA as avatar and the English VA's name in the role subtitle (e.g. "Main · EN: Bryce Papenbrook") — all information kept, no duplicates |
| **v43** (2026-07-25) | **Two brand-new sources, both live-verified end-to-end: FM FTP + Mediaserver** (user-requested, shipped as standalone extensions first — the reference implementations — and ported 1:1 into Wizstream's resolver batch, including the alternate-title anime pass). (1) **FM FTP** (`fmftp.net`) — a React app over a public JSON API: search covers its whole catalogue (movies and shows in one call), movie details carry the direct file path (one catalogue row per file, so a film's 480p/720p/1080p copies all surface as separate quality chips), and TV episodes are found by walking the server's own file index — season folders even carry the quality tag ("Season 1 1080p"), which lands on the chip since the episode filenames don't say it. (2) **Mediaserver** (`103.225.94.27/mediaserver`) — a plain WordPress video library where movies *and* single episodes ("One Piece S01E08") are sibling posts; the player config carries direct mp4 URLs. Wizstream's series matching requires the exact SxxEyy token *and* a title match on the remainder, so "One Piece S01E08" can never answer a Season-2 request. Both sites sampled live: direct files answer HTTP 206 (seekable, no login). **Caveats:** Mediaserver is a raw-IP BDIX host — playback still depends on your ISP's route to it; FM FTP shows whose season folders are missing a season simply emit nothing for it. Repo grows to 8 extensions (both newcomers Bengali-tagged, like the rest) |
| **v42** (2026-07-25) | **CineplexBD fully confirmed — temporary diagnostics retired; changelog moved to this file.** Movies AND series resolve with per-quality chips (every quality the site files as a separate item is fetched, deduped across posts, and chip-labelled from the post title when the stream itself can't prove a resolution) and with subtitle/audio tracks surfaced from HLS manifests and player configs. The ⓘ DIAG experiment (v35–v41) is gone for good — a resolver crash now only writes a log line. Housekeeping: changelog moved out of `README.md` into `CHANGELOG.md`, back-filled to v18 (the first version with shipped notes; v1–v17 predate the table — see the reconstruction note below) |
| **v41** (2026-07-25) | **CineplexBD: every quality the site has (they file each one as a separate item) + the series death-branch identified.** (1) **All matching posts are now fetched, not just the best one** — user-confirmed site structure: one movie with three qualities = three separate search cards, each with its own single-quality player. Wizstream now resolves up to 6 matching movie posts (4 for series), dedupes identical stream URLs across them, and stamps each link's quality chip from the post's title label whenever the stream itself can't prove a resolution — so *"Disclosure Day"* shows 1080p/720p/480p chips exactly like the site's item list. Inner diagnostic chips now come only from the first candidate, so multi-post fetching can't spam pinned chips. (2) **Series death-branch found via the user's v40 screenshot**: a lone "all 1 candidates tried" chip with *no* inner stage chip meant the stored episode path (meta-JSON) died inside the junk-drop hygiene — v41 no longer gives up there; it falls through to scraping the real episode page (fresh player URL), then the constructed `player.php?…&season=S&ep=E` candidates |
| **v40** (2026-07-25) | **CineplexBD: real quality chips, selectable tracks, and series that actually resolves.** (1) **Quality tags** — every CineplexBD HLS manifest is now fetched and parsed: a master playlist fans out into one link per quality rung with the true resolution chip (1080p/720p/…); a demuxed master (separate audio group) keeps the single-link form with the top-variant chip and ExoPlayer-selectable audio; only a genuine single-rendition stream stays chip-less (the site simply offers nothing else). (2) **Track selection** — subtitle tracks advertised in the HLS master (`#EXT-X-MEDIA`) and in the player config (JWPlayer `tracks:[{file:…}]`, artplayer `subtitle:{url:…}`) are now forwarded as `[CineplexBD] …` subtitles; embedded audio/subtitle groups surface in the player's track selector. (3) **Series fetching** — episode pages were scraped only at their surface HTML, but CineplexBD keeps the real stream one more server round-trip away (the same `player.php` hop the movie flow uses). The scraper now recurses one level into player/embed/ajax sub-page URLs found on the episode page (never into neighbouring episodes — wrong-episode media is impossible), and when that comes up empty it also tries the constructed `player.php?id=…&season=S&ep=E` candidate directly. A 200-that-isn't-a-playlist (the site's catch-all junk) is dropped instead of emitted ✓ error hygiene intact |
| **v39** (2026-07-24) | **The CineplexBD 2004 root cause, solved for good.** The v38 self-probe chip exposed it: media URLs were being *concatenated onto the whole player-page URL* (`…/player.php?id=76263/ondemand/<hash>/index.m3u8` — an address that cannot exist; the site's catch-all answers it with an HTTP-200 HTML page, which the player rejects as `HTTP 2004`). Relative references now resolve by RFC 3986: root-relative → against the origin, document-relative → against the page's parent path with the query string stripped. Verified with a 10-case unit suite covering the exact failing URL, token-signed HLS master variants (bonus: Sirius/HDGhar audio-track resolution), origin-only and directory bases. Temporary probe chips retired — the stage-diagnostic chips stay until series is confirmed |
| **v38** (2026-07-24) | **Movie-2004 fix candidate + media self-probe chips + series speed caps.** (1) Direct-video links now carry the resolver's session headers (incl. the player-page Cookie) — previously the player fetched the file bare and CineplexBD's CDN could reject it with `HTTP 2004` even though the resolver's own scrape succeeded. (2) When CineplexBD emits a link it now also probes that exact URL with the link's headers and pins a top chip reading `movie/tv probe → HTTP <code> · <first 90 chars of URL>` — one screenshot proves whether a 2004 means a dead URL or a header mismatch. (3) Series latency hard-capped: 6s per subtitle-manifest fetch, and only the first HLS manifest of a page is scanned for subtitle tracks (was every variant) — the series chain can no longer stall for minutes |
| **v37** (2026-07-24) | **Language → Bengali + unmissable CineplexBD diagnostics.** (1) Both Wizstream providers now declare `lang = bn`, matching the five standalone BDIX extensions (CTGMovies, Cineplex BD, Circle FTP, FTPBD, FlixHub) which already were. (2) The temporary ⓘ DIAG chips now pin to the **very top** of the source list and use stage-unique URLs, so on series titles that produce nothing you can see exactly where CineplexBD stops. (3) Series last-resort: a guarded S1E1-only landing-page scrape (mirrors the standalone's "Watch" placeholder episode — bounded so it can never serve the wrong season). **Confirmed working from the user's screenshot (v36)**: CineplexBD movies play (`Wizstream • CineplexBD - Direct`) with `[CineplexBD] English` subtitles attached; Bingr Sirius & Moonflix HDGhar quality tags visible |
| **v36** (2026-07-24) | **Quality tags for Sirius/HDGhar + an unmissable CineplexBD crash net.** (1) **Bingr Sirius and Moonflix HDGhar links now carry proper quality chips** — both CDNs (same streamraiwind family) serve *per-quality demuxed HLS masters*, whose audio tracks force the single-master emission path that previously stamped `Unknown` quality. The chip now shows the master's top resolution (which equals the API's own quality label — verified live: Sirius `1080p`/`720p`). (2) **CineplexBD diagnostics hardened**: resolvers can now opt in to a crash net at the dispatcher level — if CineplexBD's resolver dies with an exception anywhere (even before its own diagnostic points), a `Wizstream • CineplexBD ⓘ DIAG: resolver crashed (<ErrorName>)` chip still appears. Coupled with v35's per-stage chips, a completely silent CineplexBD is now impossible *if the build is actually installed and the resolver runs* |
| **v35** (2026-07-24) | **CineplexBD diagnostic build.** CircleFTP is fully healthy as of v34; CineplexBD still produced zero links on the user's device while the standalone works — and with no way to reach the BDIX site from outside Bangladesh, the resolver can't be debugged blind. v35 therefore makes CineplexBD *talk*: whenever it gives up it now leaves a clearly-labelled, never-playable source chip (`Wizstream • CineplexBD ⓘ DIAG: …`, always at the bottom of the source list) that names the exact stage it stopped at — search unreachable, HTTP error, no cards parsed, no title match, episode list empty, episode page scrape found 0 media, or an internal crash. Also: request headers are now byte-identical to the standalone extension's (same Chrome UA). **ⓘ DIAG chips are temporary** and will be removed as soon as the failure stage is identified |
| **v34** (2026-07-24) | **CircleFTP 2004 root-caused & CineplexBD series revived — with user-supplied site data as proof.** (1) **CircleFTP `HTTP 2004` fixed**: the port rewrote every CDN hostname (`ftp17.circleftp.net`) to its raw BDIX IP (`15.1.3.8`) unconditionally — but the standalone only does that swap when the raw-IP API mirror is actually in use, and in practice always emits the hostname form (verified against the site's own watch page, whose player and download anchors all use the hostname). The CDN refuses the IP-form on vhost-less requests → player 404 → `HTTP 2004`. Wizstream now emits the hostname URL exactly like the standalone, and only falls back to IPs when the IP mirror genuinely answered. (2) **CineplexBD series produced zero links**: episode selection was fixed in v33, but the matched episode URL (`watch.php?…&season=…&ep=N`) was then routed into the generic extractor loader — which knows nothing about cineplexbd — so every episode died silently. The standalone instead *fetches the episode page and scrapes the real media URL out of its HTML*; Wizstream now does exactly that for every non-direct path, with the standalone's cookie+Referer forwarding. (3) **CineplexBD subtitles**: subtitle tracks advertised on episode/player pages (`<track>`, `.srt/.vtt/.ass`) and inside HLS master manifests (`#EXT-X-MEDIA`) are now collected as `[CineplexBD] …` subtitle files, same as the standalone. (4) Watch-page episode anchors now respect their own `&season=N` marker, so a leftover Season-1 strip can never answer a Season-2 request |
| **v33** (2026-07-24) | **BDIX parity round 3 — CircleFTP & CineplexBD rebuilt to match the standalone extensions 1:1.** (1) **CircleFTP**: streams are now emitted *exactly* like the standalone extension — bare links, no forced referer/headers, quality read from the file name. The old port attached extra headers and pre-fetched every HLS playlist, which made the CDN reject requests that the standalone plays cleanly (the `HTTP 2004` errors). The last-resort “dump every episode from every season” fallback is gone for good — it was the source of wrong-season/error-full link batches and timeouts. (2) **Multi-season anime mapping**: Wizstream now walks your anime's prequel chain on AniList (counting real seasons, skipping `Part 2`/`Cour 2` splits) so “Season 4”-structured posts on CircleFTP/FTPBD line up with the right AniList entry — e.g. Attack on Titan's final entry now asks the site for its Season 4 content instead of Season 1. (3) **CineplexBD**: direct `/Data/` video paths are recognized (extensionless MKV/mp4 streams), missing episode numbers are now read from titles like “E04”, the numeric 1–12 season sweep only kicks in when the requested season returned nothing at all, and its results are only trusted when exactly one alternate season has content — so a mislabeled post can't silently serve you Season 1's first episode anymore |
| **v32** (2026-07-24) | **BDIX deep-fix round 2** — (1) **CTGMovies had silently worked for movies only**: its API returns *bare JSON arrays* for TV/anime searches while movies come wrapped in an object — the parser only understood the object, so every series/anime query returned zero. Parses both shapes now, reads the flat `episodes[]` layout (with per-episode `links[]` + `season_number`) that the live `/tv` and `/anime` endpoints actually return, and prefers `hls_url` when a link offers one. (2) **CineplexBD retry ladder**: strict title-matching stays the first tier, but if it matches nothing the resolver now falls back to a relaxed year-compatible match and tries the **top-3 matching posts** until one actually plays (a broken first pick used to poison the whole resolve). (3) **CircleFTP** gets the same relaxed second-chance tier. (4) **FTPBD** no longer collects WordPress `280x176`-style thumbnail attachments disguised as `.avi` media files. (5) **Allmanga**: added the current site's own one-call crypto bootstrap (`/client-crypto/v1/bootstrap`) as a key source, and `api-t.mkissa.net` as a clock.json host, so the internal CDN sources light back up by themselves once AllAnime's servers recover from their ongoing outage. Viewer-focused README refresh |
| **v31** (2026-07-23) | **BDIX parity fixes**: FTPBD series/anime episode pages (slug URLs with no S/E numbers) now resolved via the standalone's numbered-grid scrape (verified live: One Piece S1E5 → HLS, Dune → 3 links); identity gate learned audio/rip decorations ("ONE PIECE Hindi Dubbed" etc.) so legit posts stop being rejected; single-letter romans no longer corrupt titles; BDIX resolvers get a second search pass with the anime's alternate (romaji/English) title; CineplexBD gained the standalone's season-probe + watch-page fallbacks; anime cast expanded to 25 main+supporting characters with Japanese **and** English voice actors |
| **v30** (2026-07-23) | **Cineby rebuilt for the site's June-2026 relaunch** — new official domain `cineby.at`, new aggregate `/mbx` endpoint, new in-browser “mvm1” stream cipher ported 1:1 to Kotlin (verified byte-for-byte against the site's JS with test vectors, incl. a rotation-formula gotcha that breaks naive ports). **Yoru is off the entire internet** — its CDN domain (cdntv.one) expired and is parked, so no client can play it; parked-CDN/ad-wall answers are now dropped silently. Subtitles from `subs.videasy.to` with `[Videasy]` labels |
| **v29** (2026-07-23) | **Anime title-mismatch fix**: Allmanga's index is romaji/alias-based while AniList feeds English titles — matching now uses every server-side alias × both AniList titles (One Piece ↔ `1P` etc.); every anime source retries with the romaji alias |
| **v28** (2026-07-23) | Neon resurrection (scheme-less junk-CDN playlist URIs mangled by the resolver, 404'd for weeks); demuxed HLS masters emitted as adaptive master so audio stays muxed; homepage TV rows fixed (missing leading slashes); long-running anime episode counts un-capped (AniList `episodes=null`) |
| **v27** (2026-07-23) | Device codec gate: `MediaCodecList` decides what your hardware can actually decode and unplayable links are never offered (kills ExoPlayer **4003** on TVs); all HLS masters expanded into per-resolution rows; codec tags simplified |
| **v26** (2026-07-23) | Self-descriptive link names: “Source · Server — audio/codec tags” in the player switcher |
| **v25** (2026-07-22) | Health sweep: removed 4 dead embed hosts + SmashyStream extractor; embed-page fallback links removed (3003 fixes); FlixHub icon; README/repo.json reorganised |
| v24 (2026-07-22) | Added **Bingr** (7 servers) + **Moonflix** (3 backends incl. Hindi-dub mp4 ladder); Cineby re-verification |
| v23 | Movie sources trimmed to BDIX + Cineby; Miruro removed |
| v22 | **Allmanga** added (AllAnime family, self-bootstrapping crypto) |
| v21 | TokyoInsider resolver (direct MKV/MP4 anime downloads) |
| v20 | BDIX fuzzy-pick revert (stability) |
| v19 | Mkissa AA-crypto bypass; UniqueStream · AniNeko · ReANIME; probe gate |
| v18 | Codec tagging groundwork for TV decoder errors |

---

## Earlier history (v1 – v17)

The changelog table only began at v18 — and even that era was shorthand.
No shipped notes survive for v1–v17. What can honestly be reconstructed
from code comments that still live in the earliest baseline:

- **v1–v10** — early Wizstream unified-plugin era plus the first
  standalone BDIX extensions (CTGMovies, Cineplex BD, Circle FTP, FTPBD,
  FlixHub); no notes were kept.
- **v2** — CTGMovies already present and working (from a surviving code
  comment: “already worked in v2”).
- **v11** — CircleFTP multi-season mapping fix (surviving “FIX v11”
  comment in the season-fallback code).
- **v12** — CineplexBD movie filter: only `/view.php` pages count as
  movies, `/watch.php?...` is a series page.
- **v17** — CircleFTP title-similarity filtering experiments (relaxed
  again later).

*(If you still have personal notes from v1–v17, they belong here —
send them and they'll be back-filled.)*
